import PassportClient from "./PassportClient";

export const metadata = {
  title: "Passport Size Photo (KB, Pixels, CM) + Free Tool | FixMyDocs",
  description:
    "Check passport size photo dimensions in pixels, cm, and KB. Resize and compress image for passport and government forms online free.",
};

export default function Page() {
  return <PassportClient />;
}