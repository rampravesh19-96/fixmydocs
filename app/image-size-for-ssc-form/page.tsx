import SSCClient from "./SSCClient";

export const metadata = {
  title: "SSC Photo & Signature Size (KB, Pixels) + Free Tool | FixMyDocs",
  description:
    "Check SSC photo and signature size requirements in KB, pixels, and cm. Compress and resize image for SSC form online free.",
};

export default function Page() {
  return <SSCClient />;
}